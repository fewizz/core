/*#include "division_with_remainder.hpp"
#include <cassert>

int main() {
	//division_result res{0,0};

	division_result res = divide_with_remainder(1, 3);
	assert(res.quotient == 0 && res.remainder == 1);

	division_result res0 = divide_floot_with_remainder(4, 3u);
	assert(res.quot == 1 && res.rem == 1);

	res = u::div_floor(-1, 3);
	assert(res.quot == -1 && res.rem == 2);

	res = u::div_floor(-4, 3);
	assert(res.quot == -2 && res.rem == 2);
}*/